# hash-it
A simple, lightweight Python command-line tool that lets you generate different cryptographic hashes (like MD5, SHA-1, SHA-256, and more) for any file. It’s handy when you want to check file integrity or do some basic security checks.
